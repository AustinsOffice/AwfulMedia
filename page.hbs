{{!< default}}
{{#post}}
    <div class="row">
        <article class="col-md-12 post">
            <h1 class="post-title">{{title}}</h1>
            {{content}}
        </article>
    </div>
{{/post}}
