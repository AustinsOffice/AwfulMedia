{{!< default}}
{{#post}}
    <section class="post row">
        <article class="col-md-12">
            <h1 class="post-title">{{title}}</h1>
            {{content}}
        </article>
    </section>
{{/post}}
